// LOOPS AND CONDITIONAL

// Declaring a variable with an initial value
let count = 0;

// Defining the target value
const target = 12;

// while loop with a condition to increment the variable
while (count < target) {
  console.log(count); 
  count++; 
}

// After the loop completes, you can log the final value
console.log("Final value:", count);
