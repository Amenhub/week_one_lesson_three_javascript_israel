// LOOPS AND CONDITIONAL

// Declaring a variable with an initial value
let count = 0;

// Define the target value
const target = 8;

// do...while loop 
do {
  console.log(count); // Log the value of the variable
  count++; // Increment the variable
} while (count < target);

// After completing the loop
console.log("Final value:", count);
