// LOOPS AND CONDITIONAL

// Create an array of strings
let phones = ["techno", "infinix", "samsung", "iphone", "redmi"];

// Use a for loop to iterate over the array in reverse
for (let i = phones.length - 1; i >= 0; i--) {
  console.log(phones[i]);
}
